stages:
  - test
  - build

image: node

default:
  before_script:
    - echo $PWD
    - ls
    - ls node_modules
    - which yarn
    - yarn --version
    - yarn

test:
  stage: test
  script:
    - yarn test

build:
  stage: build
  script:
    - yarn build
